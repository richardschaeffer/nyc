
If you use Eclipse, you can easily open the project in that IDE and Run normally (F11).

Unit Tests can easily be run with jUnit5 inside of Eclipse.

Alternatively, you can browse to the directory and run on command line with 
"javac SentenceAttributes.java"